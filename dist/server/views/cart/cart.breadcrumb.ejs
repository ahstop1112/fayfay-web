<section class="breadcrumb-section">
    <div class="container-fluid">
        <div class="breadcrumb-container">
            <a class="breadcrumb" href="<%= langUrl %>/" title="<%= translations['common.home'] %>"><%= translations['common.home'] %></a>
            <span class="breadcrumb arrow"></span>
            <a class="breadcrumb active"><%= translations['shopping.cart'] %></a>
            <span class="breadcrumb arrow"></span>
            <span class="breadcrumb"><%= translations['common.checkout'] %></span>
            <span class="breadcrumb arrow"></span>
            <span class="breadcrumb"><%= translations['payment.done'] %></span>
        </div>
    </div>
</section>
<h4>
    <FormattedMessage id="shopping.cart"/>
</h4>
